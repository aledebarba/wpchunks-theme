{
  "name": "js-bundle-test",
  "version": "0.0.1",
  "description": "JS Bundled Component - wpchunk component model",
  "source": "./src/index.js",
  "app": "build/index.js",
  "browserslist": "> 0.25%",
  "engines": {
    "browsers": "> 0.25%"
  },
  "targets": {
    "app": {}
  },
  "keywords": [
    "wpchunk",
    "wpchunk-component",
    "javascript"
  ],
  "devDependencies": {
    "parcel": "^2.6.2",
    "@parcel/transformer-sass": "^2.6.2",
    "browser-sync": "^2.27.10",
    "sass": "^1.54.0"
  },
  "scripts": {
    "start": "parcel watch && rm -rf .cache && rm -rf .parcel-cache",
    "build": "parcel build && rm -rf .cache && rm -rf .parcel-cache",
    "clear": "rm -rf .cache && rm -rf .parcel-cache && rm -rf node_modules",
    "bs": "browser-sync start --files './src' --proxy "
  },
  "license": "MIT",
  "wpchunk": "jsbundle"
}
